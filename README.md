# Project Management Service (Go + PostgreSQL)

### Запуск:

```bash
make run
API endpoints:
GET /users — список пользователей
POST /users — создать пользователя
DELETE /users/{id} — удалить пользователя
Аналогично /tasks и /projects
```
